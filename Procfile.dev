web: bin/rails server -p $(if [ -z "$CYPRESS" ]; then echo 3000; else echo 5017; fi) -P $(if [ -z "$CYPRESS" ]; then echo "tmp/pids/dev_server.pid"; else echo "tmp/pids/cypress_server.pid"; fi)
js: yarn build --watch
css: yarn build:css --watch